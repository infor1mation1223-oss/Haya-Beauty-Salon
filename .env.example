PORT=3000
NODE_ENV=production

# Optional. Used for canonical URLs and sitemap if the request host should be overridden.
SITE_URL=

# Optional. If set, each booking request is also POSTed as JSON to this URL.
BOOKING_WEBHOOK_URL=

# Optional. If set, each contact message is also POSTed as JSON to this URL.
CONTACT_WEBHOOK_URL=
